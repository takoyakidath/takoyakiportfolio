import type { Config } from "tailwindcss"

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        "mochiy-pop": ['"Mochiy Pop One"', "sans-serif"],
      },
    },
  },
  plugins: [],
}

export default config
